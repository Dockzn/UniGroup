// Verifica se o usuário está logado
let isLoggedIn = false;

function checkLoginStatus() {
  // Substituir pela lógica de verificação
  return isLoggedIn;
}

// Redireciona se estiver logado
if (checkLoginStatus()) {
  window.location.href = 'pages/inicio/inicio.html';
}